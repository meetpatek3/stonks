/** The route segment passed to mcp-handler's basePath option. */
export const MCP_BASE_PATH = "/api/mcp";

/** The streamable HTTP endpoint derived by mcp-handler from MCP_BASE_PATH. */
export const MCP_CONNECTION_PATH = `${MCP_BASE_PATH}/mcp`;
